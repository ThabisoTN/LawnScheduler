@model IEnumerable<LawnScheduler.Data.Booking>

<h2>My Bookings</h2>

<div class="container">
    <table class="table table-striped table-bordered booking-table">
        <thead>
            <tr>
                <th>Booking ID</th>
                <th>Machine Name</th>
                <th>Scheduled Date</th>
                <th>Status</th>
                <th>Email of Operator You Booked</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var booking in Model)
            {
                <tr>
                    <td>@booking.BookingId</td>
                    <td>@booking.Machine.MachineName</td>
                    <td>@booking.ScheduledDate.ToShortDateString()</td>
                    <td>@booking.Status</td>
                    <td>@ViewBag.OperatorEmails[booking.Machine.OperatorId]</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    /* Header Style */
    h2 {
        margin-bottom: 20px;
        font-size: 2rem;
        font-weight: bold;
        text-align: center;
        color: #264653;
    }

    /* Table Styles */
    .booking-table {
        width: 100%;
        margin-top: 20px;
        border-radius: 0.5rem;
        overflow: hidden;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
    }

    .booking-table th,
    .booking-table td {
        padding: 15px;
        text-align: left;
        border: 1px solid #dee2e6;
    }

    /* Header Row */
    .booking-table th {
        background-color: #2a9d8f;
        color: #fff;
        font-weight: bold;
    }

    /* Table Row Styling */
    .booking-table tbody tr:nth-of-type(odd) {
        background-color: #f8f9fa;
    }

    .booking-table tbody tr:nth-of-type(even) {
        background-color: #ffffff;
    }

    .booking-table tbody tr:hover {
        background-color: #e9ecef;
        cursor: pointer;
    }

   
    
</style>
